{% extends "index.twig" %}

{#
	Category, tag, date and CPT archives all reuse the index layout. Only the
	heading differs, and Timber already puts the archive title in the context.

	`work` archives will need their own template once work-index is built —
	a project grid is not a post list.
#}
